---
sidebar_position: 12
title: Quick Start
---

# Quick Start

A minimal example that demonstrates how to use `3kdb` in a project.

1. Clone and install the library (see Installation).

2. Import `3kdb` into your project and run a simple example. The repository contains `examples/` — I can import representative examples into this site if you want.

Example placeholder (language agnostic):

```text
# pseudo-code
import 3kdb

db = 3kdb.connect(...)
print(db.status())
```

Tell me which language/runtime `3kdb` targets and I will replace the placeholder with a real code example.
